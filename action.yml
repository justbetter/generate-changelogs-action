name: 'Changelog Generator'
description: 'Automatically generate a CHANGELOG based on the releases in GitHub'

author: Vincent Boon <vincent@justbetter.nl>

inputs:
  owner:
    required: true
    description: The GitHub owner of the repo github.com/OWNER/repo
  repo:
    required: true
    description: The GitHub repository of the repo github.com/user/REPO

branding:
  icon: copy
  color: purple

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.owner }}
    - ${{ inputs.repo }}
